html
  head
    title my first webpage with express
  body
    - var my_array = [1,2,3,4,5];
    ul
      // This is a comment
      - for (var i = my_array.length -1; i>=0 ; i--)
        li
          - if (i==2)
            b=my_array[i]
          - else 
            b
              p(style="color:red;")=my_array[i]
          p Test: #{my_array[i]} click on #[a(href="testing.com") this is a link] ok
          // li!= "<b>"+my_array[i]+"</b>" <-- be careful with xss prevention
    h1!=info
    p my first webpage with express 
        b!=info
    input(type="text", value=info)
